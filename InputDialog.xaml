<Window x:Class="DshControl.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Width="540" Height="215"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource BgAppBrush}"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        Icon="dsh-control.ico">
    <Grid Margin="20,18,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Style="{StaticResource TbBody}" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <TextBox x:Name="InputBox" Grid.Row="1" Style="{StaticResource InputStyle}"
                 KeyDown="InputBox_KeyDown"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="BtnOk" Content="确定" Style="{StaticResource BtnPrimary}"
                    Width="88" Margin="0,0,8,0" Click="BtnOk_Click"/>
            <Button x:Name="BtnCancel" Content="取消" Style="{StaticResource BtnSecondary}"
                    Width="88" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
